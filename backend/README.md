# Backend

Express + SQLite REST API for Smart Degree Planner. See the [project root README](../README.md) for the full API reference, architecture notes, and setup instructions.

Quick start (from this directory):

```bash
npm install
cp .env.example .env
npm run dev
```

Requires Node 22.5+ (uses the built-in `node:sqlite` module — no native build step).
