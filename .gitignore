# npm workspaces hoist shared deps to the root node_modules
node_modules

.DS_Store
*.log
*~

# Environment files (see .env.example in each workspace)
.env
.env.local

# stray scratch file from local testing — safe to delete manually
frontend/src/utils/rmtest_delete_me.txt
